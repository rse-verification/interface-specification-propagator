[kernel] Parsing isp_008_interface_spec.c (with preprocessing)
[isp] No entry point was specified! The default entry point "main" will be used.
[isp] Execute Eva with entry point "main"
[eva] Analyzing an incomplete application starting at main
[eva] Computing initial state
[eva] Initial state computed
[eva:initial-state] Values of globals at initialization
  X ∈ [--..--]
  Y ∈ [--..--]
[eva:alarm] isp_008_interface_spec.c:22: Warning: 
  function main, behavior interface_spec: precondition X ≥ -5 got status unknown.
[eva:alarm] isp_008_interface_spec.c:23: Warning: 
  function main, behavior interface_spec: precondition X ≤ 10 got status unknown.
[eva] done for function main
[eva:summary] ====== ANALYSIS SUMMARY ======
  ----------------------------------------------------------------------------
  3 functions analyzed (out of 3): 100% coverage.
  In these functions, 7 statements reached (out of 7): 100% coverage.
  ----------------------------------------------------------------------------
  No errors or warnings raised during the analysis.
  ----------------------------------------------------------------------------
  0 alarms generated by the analysis.
  ----------------------------------------------------------------------------
  No logical properties have been reached by the analysis.
  ----------------------------------------------------------------------------
[isp] Eva analysis is completed.
[isp] The transformed source code:
/* Generated by Frama-C */
int X;
int Y;
/*@ behavior isp_generated:
      assumes \true;
      requires x ≥ -5;
      requires x ≤ 10;
      ensures \result ≥ -15;
      ensures \result ≤ 30;
      assigns \nothing;
 */
int triple(int x)
{
  int __retres;
  __retres = x * 3;
  return __retres;
}

/*@ behavior isp_generated:
      assumes \true;
      requires t ≥ -15;
      requires t ≤ 30;
      requires \valid_read(&Y);
      requires \valid(&Y);
      ensures Y ≥ -15;
      ensures Y ≤ 30;
      assigns Y;
 */
void set_y(int t)
{
  Y = t;
  return;
}

/*@ behavior interface_spec:
      assumes \true;
      requires \valid_read(&X);
      requires \valid_read(&Y);
      requires \valid(&Y);
      requires X ≥ -5;
      requires X ≤ 10;
      ensures Y ∈ (-15 .. 30);
      assigns Y;
    
    behavior isp_generated:
      assumes \true;
      requires X ≥ -5;
      requires X ≤ 10;
      requires \valid_read(&Y);
      requires \valid(&Y);
      requires \valid_read(&X);
      ensures Y ≥ -15;
      ensures Y ≤ 30;
      assigns Y;
 */
void main(void)
{
  int t = triple(X);
  set_y(t);
  return;
}


